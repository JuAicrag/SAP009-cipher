
export default {
function cifrar()= {
    encode: function (mensagemCifra, desloc){
        const mensagemResult = ""
        for(let i=0; i<mensagemCifra.lenght; i++)
        

    }
    
    decode:
};
}